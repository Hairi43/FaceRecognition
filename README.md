# FaceRecognition
Project for live and recorded face recognition. PWr 2025

# Usage
Create folders `imagesDB, recordingsDB, scoresDB` in repository root. That's where files will be stored.

To run app use this command ```python3 gui.py```


# Reference
This project uses Facex-zoo library.

